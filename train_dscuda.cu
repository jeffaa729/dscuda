// Provides the standalone C/CUDA entry point for configuration loading, memory planning, checkpointing, training, evaluation, and generation.
// It orchestrates model forward and backward execution, optimizer updates, logging, and optional architecture-specific kernel backends.
