// Applies Rotary Positional Embeddings to configured query and key dimensions using position-dependent complex rotations.
// Its backward path rotates output gradients by the inverse angle to recover gradients for the unrotated inputs.
