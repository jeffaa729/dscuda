// Implements the AdamW optimizer update using first- and second-moment estimates with decoupled weight decay.
// It updates mixed-precision model parameters from accumulated gradients after clipping and overflow checks are complete.
