// Implements V4 manifold-constrained hyper-connections that mix several parallel residual streams around attention and feed-forward sublayers.
// Its backward path propagates gradients through the input, output, and constrained residual-stream mixing transformations.
