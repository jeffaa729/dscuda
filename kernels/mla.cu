// Implements Multi-head Latent Attention with low-rank query and KV compression, partial RoPE, and memory-efficient causal attention.
// It provides training forward and backward paths together with an inference path that stores compressed latent KV cache entries.
